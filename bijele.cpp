#include <iostream>
#include <cmath>

using namespace std;

int main() {
    const int k=1, q=1, r=2, b=2, kn=2, p=8;
    int k1, q1, r1, b1, kn1, p1;
    cin >> k1 >> q1 >> r1 >> b1 >> kn1 >> p1;
    
    cout << k-k1 << " " << q-q1 << " " << r-r1 << " " << b-b1 << " " << kn-kn1 << " " << p-p1;
}
